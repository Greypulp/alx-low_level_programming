#include <stdio.h>
/**
 * main - Prog Entry point
 * Return: Always (0) (Success)
 */
int main()
{
	char = c;
	
	c = 'a';
	while
		(c <= 'z') {
		if  ((c != 'q' && c != 'e') && c <= 'z')
			putchar(c);
		c++;
		}
	putchar('\n');
	return (0);
}
