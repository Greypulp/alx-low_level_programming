#include "main.h"
/**
* print_abs - computes the absolute value of an integer
* @a: function parameter
* Return: 0
**/
int _abs(int a)
{
	if (a < 0)
		return (-a);
	else if (a >= 0)
	{
		return(a);
	}
}
